mlflow_params:
  run_name: 'baseline_random_forest'

pipeline_params:
  label_col: 'churn'
  test_size: 0.25
  random_state: 42

model_params:
  n_estimators: 200
  max_depth: 8
  min_samples_leaf: 1
  max_features: 'auto'
  random_state: 42